#include "ei_types.h"
#include "ei_widget.h"

/* Draws the widget recursively
 */
void ei_draw_widget(ei_widget_t * widget);

/* Free the widget recursively
 */
void ei_free_widget(ei_widget_t * widget);
